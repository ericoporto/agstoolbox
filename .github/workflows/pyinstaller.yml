name: Pyinstaller Win Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Package Application
      uses: ericoporto/pyinstaller-action-windows@main
      with:
        spec: agstoolbox.spec
        path: .

    - name: Package Application
      uses: ericoporto/pyinstaller-action-windows@main
      with:
        spec: atbx.spec
        path: .

    - uses: actions/upload-artifact@v2
      with:
        name: agstoolbox
        path: ./dist/windows
